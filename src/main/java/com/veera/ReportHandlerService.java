package com.veera;

import org.springframework.stereotype.Service;

/**
 * Maintain success/failure ids status here.
 * Also handle the retry if the authentication token is expired based on the exception type or message or status code
 */
@Service
public class ReportHandlerService {
}